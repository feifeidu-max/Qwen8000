# RTX8000 Qwen3.8-27B 模型使用文档

> 更新：2026-08-23 ｜ 服务位置：远程服务器 rtx8000 的 GPU1 ｜ 本机：Windows + WSL2
>
> 📌 **长期规则：每次测试/验证结束后必须释放 GPU**（执行第四节停止命令并确认
> 显存回到 6 MiB），不允许让模型在无人使用时空转占卡。

## 一、架构与访问链路

```
Windows 浏览器 (pi-web: http://127.0.0.1:30141)
        │  Node.js 发起 OpenAI 兼容请求 (UTF-8)
        ▼
http://127.0.0.1:8000/v1   ←── WSL2 自动把 WSL 内监听端口转发给 Windows localhost
        │  SSH 加密隧道 (WSL 内 ssh -L 8000:127.0.0.1:8000 rtx8000)
        ▼  经 zicp.fun 动态域名隧道中转
rtx8000 服务器: vLLM 服务 (127.0.0.1:8000, GPU1)
   模型 = Qwen3.8-27B-AWQ-INT4, MTP3 投机解码, 最大上下文 208896 (约 204K tokens)
```

模型 API 是 **OpenAI 兼容**格式，任何支持自定义 OpenAI 端点的工具都能接。

## 二、日常使用（最短路径）

1. 开一个 WSL 终端（隧道自动重连；也可手动 `~/RTX8000/start-tunnel.sh`）
2. **启动模型（带实时进度显示）**：
   ```bash
   cd ~/RTX8000 && ./qwen-start single    # 单卡日常推荐
   # 或 ./qwen-start dual                 # 双卡模式
   ```
   就绪后会输出摘要框（API 地址 / 模式 / KV 池 / PID / 日志路径）
3. Windows 浏览器打开 pi-web：`http://127.0.0.1:30141`
4. 会话内用 `/model` 选 **rtx8000 → Qwen3.8-27B AWQ INT4 (RTX8000)** 即可对话

> 底层命令与原理见第三节；`qwen-status` 可随时查看隧道/进程/显存/API 状态。

## 三、远程模型的启动与关闭

以下为底层命令（等效于 qwen-start 的内部实现，供排查和自定义）。

### 启动 / 重启模型

两种模式二选一。所有命令在 WSL 终端执行。

---

**【模式一 · 单卡】TP=1 + MTP3 + 多模态 + 前缀缓存**
占用 GPU1（~45GB）。decode 最快（MTP3 加速），适合日常对话 / pi-web 编码。
加载约 3~5 分钟。

```bash
# 普通启动
ssh rtx8000 'nohup bash /mnt/sdc/work/restart_qwen38_awq.sh > /dev/null 2>&1 &'

# 带崩溃守护的启动（推荐：崩溃自动重启）
ssh rtx8000 'setsid nohup bash /mnt/sdc/work/supervise_qwen38.sh > /mnt/sdc/work/supervisor.log 2>&1 < /dev/null &'
```

---

**【模式二 · 双卡】TP=2 + MTP3 + 多模态 + 前缀缓存**
占用 GPU0+GPU1（各 ~44GB），KV 池 103 万 tokens。长上下文 prefill +40%、
128K decode +53%；代价是超短 prompt prefill 略慢（−21%）。
加载约 5~8 分钟。⚠️ **存在启动竞态，务必用守护模式**（挂起会自动清理重试直至成功，
实测同配置一次挂一次成，重试必达）。

```bash
# 双卡必须用守护启动
ssh rtx8000 'setsid nohup bash /mnt/sdc/work/supervise_tp2.sh > /mnt/sdc/work/supervisor_tp2.log 2>&1 < /dev/null &'
```

---

**守护模式共同特性**：进程崩溃 15 秒内自动拉起；启动失败/挂起自动清理重试；
用停止脚本关闭时会留下标记、守护识别后自动退出不复活。守护日志分别在
`/mnt/sdc/work/supervisor.log`（单卡）与 `/mnt/sdc/work/supervisor_tp2.log`（双卡）。
守护不随服务器开机自启（手动策略），服务器重启后需重新执行。

**就绪验证（通用）**：

```bash
ssh rtx8000 'ss -tln | grep :8000 && echo READY || echo NOT_READY'
curl -s --max-time 8 http://127.0.0.1:8000/v1/models | head -c 200
# 看到 "rtx8000-qwen38-27b-awq-int4" 即就绪
```

### 关闭模型（推荐用停止脚本，含孤儿进程兜底）

```bash
ssh rtx8000 'bash /mnt/sdc/work/stop_qwen38.sh'
# 输出 "DONE. GPU1 memory now: 6 MiB" 即完全关闭
```

- **守护联动**：停止脚本会先写入 `.qwen38_manual_stop` 标记，若守护模式在运行，
  守护进程识别到标记后自动退出，模型**不会被复活**（已实测通过）

> ⚠️ 为什么不要直接用裸 pkill：vLLM 的 EngineCore 子进程在父进程被 SIGKILL 后会被
> 系统孤儿化继续占卡（实测出现过残留进程占 46GB 显存），且手工命令容易因引号/
> 时机问题遗漏。停止脚本按名称杀完后，**再按 GPU1 实际占用进程兜底清一遍**。
>
> 重启脚本 restart_qwen38_awq.sh 开头也已内置同样的兜底清理，直接重启即可覆盖残局。
> 关闭后 pi-web 里选该模型会报连接错误，属正常现象。

### 手动应急（仅当停止脚本不可用时）

```bash
ssh rtx8000 'pkill -9 -f "vllm.entrypoints.openai.api_server"; sleep 1; pkill -9 -f VLLM::EngineCore; sleep 2'
ssh rtx8000 'nvidia-smi --query-compute-apps=pid,used_memory --format=csv,noheader -i 1'   # 检查残留
ssh rtx8000 'for pid in $(nvidia-smi --query-compute-apps=pid --format=csv,noheader -i 1); do kill -9 $pid; done'  # 兜底
```

### 查看状态与日志

```bash
ssh rtx8000 'ss -tln | grep :8000'                       # 端口是否在听
ssh rtx8000 'nvidia-smi --query-gpu=index,memory.used --format=csv,noheader'  # GPU1 应占 ~45GB
ssh rtx8000 'tail -30 $(ls -t /mnt/sdc/work/vllm-2080ti/run-logs/vllm-rtx8000-qwen38-27b-awq-int4-*.log | head -1)'
```

## 四、SSH 隧道管理（本地 WSL）

| 操作 | 命令 |
|---|---|
| 启动隧道 | `~/RTX8000/start-tunnel.sh`（幂等，重复执行无害） |
| 关闭隧道 | `~/RTX8000/stop-tunnel.sh` |
| 查看隧道 | `ss -tln \| grep 8000` |

已配置：打开任意 WSL 终端时若隧道断开会**自动重连**（~/.bashrc 尾部的钩子）。

手动测试连通性：

```bash
curl http://127.0.0.1:8000/v1/models          # WSL 内
# Windows 侧 (PowerShell): Invoke-RestMethod http://127.0.0.1:8000/v1/models
```

## 五、pi-web 接入说明

- 配置文件（已由助手写入并保留原配置备份）：`C:\Users\13129\.pi\agent\models.json`
  备份位置：WSL `~/RTX8000/models.json.backup-*`
- 新增 provider 名为 **rtx8000**，baseUrl=`http://127.0.0.1:8000/v1`，
  模型 ID=`rtx8000-qwen38-27b-awq-int4`，上下文窗口 208896
- pi 打开 `/model` 时自动重读该文件，改完无需重启 pi-web
- 若要恢复原配置：把备份复制回 `C:\Users\13129\.pi\agent\models.json`

### 多模态（图像理解）— 2026-08-22 已启用 ✅

检查点自带视觉塔（ViT），**默认启动脚本已固定为图文多模态模式**（每次启动自动带上，
无需任何额外操作）：

- 每请求最多 1 张图（`--limit-mm-per-prompt`），支持 PNG/JPEG，base64 或 URL 均可
- 显存代价极小：KV 容量 31.6 万 tokens（纯文本版为 31.9 万），204K 长上下文不受影响
- API 用法（OpenAI 兼容格式）：

```json
{"role":"user","content":[
  {"type":"image_url","image_url":{"url":"data:image/png;base64,<BASE64>"}},
  {"type":"text","text":"描述这张图"}
]}
```

- pi 配置已声明 `input: ["text","image"]`
- 启动脚本说明：`restart_qwen38_awq.sh` = **多模态版（默认）**；
  `restart_qwen38_awq_textonly.sh` = 纯文本备份版（视觉塔不加载，省 ~1GB 显存）
- ⚙️ 启动策略：**纯手动**。服务器重启后模型不会自动拉起，需按第三节命令手动启动
  （曾配置过的 crontab @reboot 自启已于 2026-08-22 按需求取消）

### API Key 策略 — 任意值均可 ✅

服务端**未开启鉴权**（未设置 --api-key 参数），以下形式全部放行：

| 调用方式 | 结果 |
|---|---|
| 不带 Authorization 头 | ✅ 200 |
| `Authorization: Bearer`（空） | ✅ 200 |
| `Authorization: Bearer sk-随便什么字符串` | ✅ 200 |

pi 配置里的 `apiKey: "rtx8000-local"` 只是占位符，其他工具接入时 key 随便填。
⚠️ 安全提示：正因如此**不要把隧道端口暴露到公网**，保持 SSH 隧道模式即可。

### 缓存命中率显示为 0 的原因与对策 — 2026-08-23 诊断

**根因**：本 fork 对混合 GDN 架构默认禁用前缀缓存（上游标记 experimental），
启动参数 `enable_prefix_caching=False`，因此所有请求 `cached_tokens` 恒为空、
pi-web 命中率恒为 0。

**实测结论（2026-08-23，强制开启后）**：

| 项目 | 结果 |
|---|---|
| 引擎能否强制开启 | ✅ 加 `--enable-prefix-caching` 正常启动 |
| 重复长 prompt TTFT | ~4s → **1.3~2s**（约 2~3× 提升） |
| 共享前缀+新问题 | ✅ 同样命中加速 |
| 结果正确性 | ✅ 命中缓存后答案正确（含唯一暗号校验） |
| cached_tokens 字段回填 | ❌ 本 fork 不上报（纯显示问题，不影响实际加速） |

**如何启用**：✅ **已默认启用（2026-08-23）**——`restart_qwen38_awq.sh` 与
`restart_qwen38_awq_mm.sh` 均已内置 `--enable-prefix-caching`，无需额外操作。
实测重复请求 TTFT 约 6× 提升（8.3s→1.4s）。恢复禁用：从脚本删除该行即可。

### 256K 上下文压力测试 — 2026-08-23 ✅ 通过

服务以 `--max-model-len 262144`（原生满配）+ util 0.96 启动，KV 池 345,xxx tokens。
GPU 峰值 47.3GB / 利用率 100%，全程零 OOM 零崩溃。测试脚本：
`rtx8000:/mnt/sdc/work/{stress256k,cold256}.py`

| 轮次 | 实际上下文 | 类型 | TTFT | Prefill | 正确性 |
|---|---|---|---|---:|---|
| R1 | 228,468 tok | 冷启动（无缓存） | 457.6s | 499 tok/s | ✅ |
| R2 | 228k 共享前缀+新尾部 | APC 命中 | **7.2s** | — | ✅ |
| R3 | 与 R1 完全相同 | APC 全命中 | **7.3s** | — | ✅ |
| TRUE256 | 255,878 tok | 部分 lorem 缓存复用 | 100.9s | — | ✅ |
| COLD256 | 254,876 tok | **冷启动最坏情况**（无重复词表） | **535.9s** | **476 tok/s** | ✅ |

- 每轮均带唯一暗号校验，5/5 回答正确
- 结论：**256K 满上下文可用且稳定**；重复/相似内容场景收益巨大（APC 命中 60×+）；
  冷启动吞吐 ~480-500 tok/s 已到平台期（与 200K 时一致）
- ⚠️ 该 262144 配置为压力测试专用（`restart_qwen38_stress_262144.sh`, util 0.96）；
  日常默认脚本仍为 208896 @ util 0.92

### 双卡 TP=2 测试 — 2026-08-23 最终版 ✅ 可用且性能全面超越单卡

**死锁真相**：非确定性启动竞态（同配置一次挂一次成），卡点在 MTP 草稿权重共享
之后的图捕获阶段。与 NVLink/拓扑无关，与 MTP×TP2 组合有关。

**解决方案**：启动竞态用守护模式自动重试即可收敛（restart 脚本轮询 10 分钟超时后
supervise 会清理重试，直至成功）。`mtp=3` 已设为 TP=2 默认。

**实测对比**（冷启动、排除缓存）：

| 指标 | 单卡 TP=1+MTP3 | 双卡 TP=2+MTP3 | 变化 |
|---|---:|---:|---|
| 4K prefill | 1068 tok/s | 845 tok/s | −21%（小请求有通信开销） |
| 128K prefill | 645 tok/s | **906 tok/s** | **+40%** |
| 4K decode | 65.9 tok/s | **81.2 tok/s** | **+23%** |
| 128K decode | 52.0 tok/s | **79.6 tok/s** | **+53%** |
| KV 池 | 31.6 万 | **103 万 tokens** | ×3.3 |

**结论**：长上下文 prefill/decode 双卡全面占优；仅超短 prompt 有小幅劣势。
日常推荐直接用双卡守护模式。

**双卡专用命令**：
```bash
# 启动（守护模式，遇启动竞态自动重试）
ssh rtx8000 'setsid nohup bash /mnt/sdc/work/supervise_tp2.sh > /mnt/sdc/work/supervisor_tp2.log 2>&1 < /dev/null &'
```
（supervise_tp2.sh 与单卡 supervise 相同逻辑但调用 tp2 脚本）

### 已知问题：finish_reason "repetition"（2026-08-23 已调优 ✅）

**现象**：生成中途停止，pi 报 `Provider finish_reason: repetition`。

**原因**：fork 内置幻觉循环保护——带工具调用的请求会被自动注入重复检测器
（原阈值：任意 1~32 token 模式连续重复 8 次即终止）。PPT/表格类任务的正常
重复格式（竖线、换行、相同结尾）会误触发。

**已调整**（serving.py）：`min_pattern_size 1→6`、`min_count 8→20`、
`max_pattern_size 32→48` —— 只有 ≥6 token 的完全相同块连续重复 20 次才判定为
真退化循环。格式化内容的合法重复不再误伤；真正的退化循环仍会被及时掐断。

**如果再次遇到**：说明模型真的陷入循环（长上下文偶发），直接重发即可；
频繁出现可继续放宽阈值或彻底禁用：
编辑服务器 `/mnt/sdc/work/vllm-2080ti/vllm/entrypoints/openai/chat_completion/serving.py`
中 RepetitionDetectionParams(...) 改为 `min_count=9999` 后重启。

### 思考模式档位（2026-08-22 新增）

模型原生支持三档思考强度，pi 的思考等级选择已做映射：

| pi 里选择 | 实际发给模型 |
|---|---|
| minimal / low | low（简短思考） |
| medium | medium（中等） |
| **high / xhigh / max** | **xhigh（最深思考，模型默认档）** |
| off | ❌ 不可用（模型模板不支持关闭思考，选了也会按默认思考） |

### 工具调用（编码 agent 必需）

服务端已启用 `--enable-auto-tool-choice --tool-call-parser qwen3_coder`（匹配 Qwen3.8 的
XML 工具格式），pi 的读写文件、执行命令等工具调用可正常工作。若重装/换启动脚本，
务必保留这两个参数，否则会报 `400: tool choice requires --enable-auto-tool-choice`。

## 六、故障排查

| 症状 | 原因与处理 |
|---|---|
| pi-web 报连接失败 / 超时 | ① 隧道断了：WSL 执行 `~/RTX8000/start-tunnel.sh`；② 远程模型没起：按第三节重启并看日志 |
| `ssh rtx8000` 报 Could not resolve hostname | zicp.fun 动态域名的 DNS 抖动，等 1~2 分钟重试（历史抖动均自行恢复） |
| curl 通但 pi-web 不出字 | 确认 pi-web 会话里 `/model` 选的是 rtx8000 的模型，而非云端模型 |
| 回复特别慢 | 正常现象：200K 满上下文 prefill 约 6 分钟；建议长文档场景配合 `/compact` 或新开会话 |
| 模型加载后立刻又挂 | 看 GPU 显存是否被其它进程占用：`ssh rtx8000 nvidia-smi` |
| Windows 重启后不通 | 重开一个 WSL 终端即可（bashrc 钩子自动拉起隧道） |

## 七、相关文件速查

| 文件 | 位置 | 用途 |
|---|---|---|
| 模型启停脚本 | rtx8000:`/mnt/sdc/work/restart_qwen38_awq.sh` | 杀旧进程+重启 vLLM |
| 运行日志 | rtx8000:`/mnt/sdc/work/vllm-2080ti/run-logs/` | 排错 |
| 基准脚本与结果 | rtx8000:`/mnt/sdc/work/bench200k.py`、`bench200k.log` | 性能复测 |
| 隧道脚本 | WSL:`~/RTX8000/start-tunnel.sh` / `stop-tunnel.sh` | 隧道管理 |
| pi 模型配置 | Windows:`C:\Users\13129\.pi\agent\models.json` | pi/pi-web 模型接入 |
